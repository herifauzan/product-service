_format_version: "3.0"
_transform: true
services:
  - name: product-catalog-service
    url: http://product-catalog-service:8080
    routes:
      - name: products
        paths:
          - /api/products
        strip_path: false
plugins:
  - name: rate-limiting
    config:
      second: 10000
      policy: local
